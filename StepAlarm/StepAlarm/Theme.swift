import SwiftUI

/// Black / white / red look shared across the app screens.
enum Theme {
    static let background = Color.black
    static let surface = Color(red: 0.118, green: 0.118, blue: 0.118)   // #1E1E1E
    static let accent = Color(red: 0.937, green: 0.325, blue: 0.290)    // #EF534A
    static let textPrimary = Color.white
    static let textSecondary = Color(white: 0.8)
}
